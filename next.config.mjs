/** @type {import('next').NextConfig} */
const nextConfig = {images: {
    domains: ['alcohome.co.il', 'shipi.b-cdn.net', 'theydream-online.com'],  // add all required domains
  },};

export default nextConfig;